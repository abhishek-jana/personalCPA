# Domain Docs

This project follows a single-context layout.

## Context
The project's domain language and current state are documented in `CONTEXT.md` at the repo root.

## Architecture Decisions
Past architectural decisions are documented as ADRs in `docs/adr/`.
