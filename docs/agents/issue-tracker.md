# Issue Tracker: GitHub

This project uses GitHub Issues to track work.

## CLI
Uses the `gh` CLI.

## Workflow
- Create issues with `gh issue create`.
- List issues with `gh issue list`.
- View issues with `gh issue view`.
